import Commit from "../../components/Commit";
import Code from "@/components/Code";

- **API**: Moved code from <code>app.ts</code> to <code>index.ts</code> to reduce redundancy
- **API**: Created <code>routes.ts</code> and moved all the module route declarations into it
