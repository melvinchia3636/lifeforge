import React from 'react'

const URL_REGEX =
  /(?:https?:\/\/)?(?:www\.)?(?:youtube\.com|youtu\.be)\/(?:playlist|watch)\?list=([a-zA-Z0-9_-]+)/

function PlaylistSection(): React.ReactElement {
  return <div>PlaylistSection</div>
}

export default PlaylistSection
