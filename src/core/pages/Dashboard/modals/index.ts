import { type ModalComponent } from '@lifeforge/ui'

import ManageWidgetsModal from './ManageWidgetsModal'

export const DashboardModals: Record<string, ModalComponent> = {
  'dashboard.manageWidgets': ManageWidgetsModal
}
